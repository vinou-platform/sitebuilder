system:
  template: 'Admin/Panel.twig'
  pageTitle: 'Vinou Admin'
  method: all
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: init
      forceLoadAll: true

system/settings/save:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: saveSetting
      forceLoadAll: true

system/settings/delete:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: deleteSetting
      forceLoadAll: true

system/settings/add:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: addSetting
      forceLoadAll: true

system/totp:
  template: 'Admin/Panel.twig'
  method: all
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: verifyTotpStep
      forceLoadAll: true

system/totp/setup:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: setupTotp
      forceLoadAll: true

system/totp/disable:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: disableTotp
      forceLoadAll: true

system/users/save:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: saveUser
      forceLoadAll: true

system/users/delete:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: deleteUser
      forceLoadAll: true

system/totp/first-setup:
  template: 'Admin/Panel.twig'
  method: all
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: firstTotpSetup
      forceLoadAll: true

system/users/update:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: updateUser
      forceLoadAll: true

system/redirects/save:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: saveRedirect
      forceLoadAll: true

system/redirects/delete:
  template: 'Admin/Panel.twig'
  method: post
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: deleteRedirect
      forceLoadAll: true

system/{name}:
  template: 'Admin/Panel.twig'
  excludeContent: true
  dataProcessing:
    admin:
      processor: admin
      function: page
      forceLoadAll: true
